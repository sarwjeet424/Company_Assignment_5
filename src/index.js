import React from 'react';
import ReactDOM from 'react-dom/client';
import QuizComp from './quizComponent/quizComponent';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
  <QuizComp/>
  </React.StrictMode>
);
